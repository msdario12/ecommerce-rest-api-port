CREATE TABLE users (
        email varchar(20) PRIMARY KEY,
        username varchar(20),
        password varchar(20),
        first_name varchar(20),
        last_name varchar(20)
);

SELECT * FROM users;
